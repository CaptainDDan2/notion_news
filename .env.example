# 반도체 뉴스 애플리케이션 환경 변수 설정

# OpenAI API 키 (필수)
# https://platform.openai.com/api-keys 에서 발급받으세요
OPENAI_API_KEY=sk-your-openai-api-key-here

# Flask 애플리케이션 설정
FLASK_APP=main_run.py
FLASK_ENV=production

# 서버 설정
HOST=0.0.0.0
PORT=5000
DOMAIN=your-domain.com
SECRET_KEY=your-very-secure-secret-key-here

# 보안 설정
HTTPS_ONLY=true
ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com
CORS_ORIGINS=https://your-domain.com

# 데이터베이스 설정 (선택적 - 기본값: SQLite)
# PostgreSQL 사용 시 아래 주석 해제 후 설정
# DATABASE_URL=postgresql://username:password@localhost:5432/semiconductor_news

# Redis 캐시 설정 (선택적)
# REDIS_URL=redis://localhost:6379/0

# 로깅 설정
LOG_LEVEL=INFO

# 크롤링 설정
CRAWL_INTERVAL_HOURS=2
MAX_ARTICLES_PER_SOURCE=10

# AI 분석 설정  
MAX_SUMMARY_LENGTH=200
MIN_PRIORITY_SCORE=3