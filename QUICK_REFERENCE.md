# 사용자 상호작용 기능 - 빠른 참조 가이드

## 🎯 핵심 변경사항

### 1️⃣ web_app.py - 8개 새로운 API 엔드포인트

```
POST   /api/bookmark                    → 북마크 생성
DELETE /api/bookmark/<article_id>       → 북마크 삭제  
GET    /api/bookmarks                   → 북마크 조회
POST   /api/comment                     → 댓글 작성
GET    /api/comments/<article_id>       → 댓글 조회
POST   /api/comment/<comment_id>/like   → 좋아요
POST   /api/article/share               → 공유 추적
POST   /api/admin/news                  → 관리자 뉴스
GET    /api/share-stats/<article_id>    → 공유 통계
```

### 2️⃣ static/script.js - 사용자 상호작용 함수

**북마크 관리**
- `toggleBookmark()` - 북마크 추가/제거
- `updateBookmarkButton()` - 버튼 상태 동기화

**댓글 관리**
- `submitComment()` - 댓글 작성
- `loadComments()` - 댓글 로드
- `likeComment()` - 좋아요

**공유 관리**
- `trackShare(type)` - 공유 추적 (kakao, link, copy)
- `copyShareLink()` - 링크 복사
- `displayShareStats()` - 통계 표시

**관리자**
- `submitAdminNews()` - 뉴스 추가

**유틸리티**
- `getTimeDifference()` - 시간 표시 ("5분 전")

### 3️⃣ templates/index.html - 모달 UI 업그레이드

```html
<!-- 공유 통계 -->
<div id="share-stats-container">
  📤 공유됨: 5 | 🔗 링크: 2 | 💬 카톡: 2 | 📋 복사: 1

<!-- 액션 버튼 -->
🤍 저장하기 | 💬 카톡 공유 | 📋 링크 복사 | 🔗 원문 보기

<!-- 댓글 입력 -->
닉네임 입력 (선택)
댓글 텍스트 입력
[댓글 작성 버튼]

<!-- 댓글 목록 표시 -->
반도체 전문가: "매우 유용한 정보입니다!" 👍 3
```

### 4️⃣ static/style.css - 새로운 스타일

- `.comment-item` - 슬라이드인 애니메이션
- `.like-btn` - 호버 효과
- `.comments-list` - 플렉스 레이아웃
- `#share-stats-container` - 파란색 강조 테두리

## 📱 사용자 흐름도

```
기사 클릭
    ↓
showArticleDetail() 호출
    ↓
    ├─ 기사 내용 로드
    ├─ updateBookmarkButton() - 북마크 상태 확인
    ├─ loadComments() - 댓글 로드
    └─ displayShareStats() - 공유 통계 표시
    ↓
모달 표시 (북마크, 공유, 댓글 UI 포함)
    ↓
    ├─ [저장하기] → toggleBookmark()
    ├─ [카톡 공유] → trackShare('kakao')
    ├─ [링크 복사] → trackShare('copy')
    └─ [댓글 작성] → submitComment()
```

## 🔌 API 호출 패턴

### 북마크 생성
```javascript
fetch('/api/bookmark', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    article_id: 1,
    notes: '메모'
  })
})
```

### 댓글 작성
```javascript
fetch('/api/comment', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    article_id: 1,
    comment_text: '좋은 정보네요!',
    nickname: '익명의 독자'
  })
})
```

### 공유 추적
```javascript
fetch('/api/article/share', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    article_id: 1,
    share_type: 'kakao' // 'kakao', 'link', 'copy'
  })
})
```

## 🧪 테스트 방법

### 1. 서버 시작
```bash
python main_run.py
# 또는
python web_app.py
```

### 2. 브라우저에서 확인
```
http://localhost:5000
```

### 3. API 테스트
```bash
python test_api.py
```

## 📊 데이터베이스 스키마

### ArticleBookmark
```
id INT PRIMARY KEY
article_id INT
notes TEXT
created_at DATETIME
```

### ArticleComment
```
id INT PRIMARY KEY
article_id INT
nickname VARCHAR(50)
comment_text TEXT
likes INT
created_at DATETIME
```

### ArticleShare
```
id INT PRIMARY KEY
article_id INT
share_type VARCHAR(20) -- 'kakao', 'link', 'copy'
created_at DATETIME
```

### AdminNews
```
id INT PRIMARY KEY
title VARCHAR(500)
content TEXT
summary TEXT
source VARCHAR(100)
priority FLOAT (기본값: 8.0)
created_at DATETIME
```

## 🎨 UI 예시

### 모달 레이아웃
```
┌─────────────────────────────────────┐
│ [기사 제목]                          × │
├─────────────────────────────────────┤
│ 출처 | 날짜 | 우선순위              │
├─────────────────────────────────────┤
│ 📤 공유됨: 5 | 🔗 링크: 2...        │
├─────────────────────────────────────┤
│ [기사 요약]                          │
│ [기사 내용]                          │
├─────────────────────────────────────┤
│ 🤍 저장 | 💬 카톡 | 📋 복사 | 🔗원문│
├─────────────────────────────────────┤
│ 💬 댓글                              │
│ ┌─────────────────────────────────┐ │
│ │ 닉네임: [_______________]       │ │
│ │ 댓글: [________________         │ │
│ │       ________________]         │ │
│ │                    [댓글 작성]  │ │
│ └─────────────────────────────────┘ │
│ ┌─────────────────────────────────┐ │
│ │ 반도체전문가           5시간 전  │ │
│ │ 유용한 정보입니다!              │ │
│ │ 👍 3                             │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

## 💾 Git 커밋

```
commit b65b730
Author: Copilot
Date:   [Timestamp]

feat: Complete user interaction features (comments, bookmarks, sharing)
```

모든 변경사항이 커밋되어 배포 준비 완료!

## 🚀 다음 단계

1. **로컬 테스트** ✓
2. **GitHub 푸시** - 준비 완료
3. **Render 배포** - 준비 완료

필요한 명령:
```bash
# GitHub 연결
git remote add origin https://github.com/username/notion_news.git
git branch -M main
git push -u origin main

# Render에서 자동 배포 설정
# render.yaml 파일 사용
```

## 📝 빠른 체크리스트

- [x] API 엔드포인트 구현
- [x] JavaScript 함수 작성
- [x] HTML UI 업데이트
- [x] CSS 스타일링
- [x] 에러 처리
- [x] Git 커밋
- [ ] GitHub 푸시 (대기 중)
- [ ] Render 배포 (대기 중)
